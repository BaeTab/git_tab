<Window x:Class="GitTab.App.Views.CommandPaletteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:GitTab.App.Localization"
        Title="Command Palette"
        Width="560" Height="420" WindowStyle="None" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{DynamicResource Brush.Surface}" BorderBrush="{DynamicResource Brush.Border}"
            BorderThickness="1" CornerRadius="8" Padding="6">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <TextBox x:Name="SearchBox" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="15" Padding="8,7"/>
                <TextBlock Text="{loc:Tr Palette.Placeholder}" Style="{StaticResource MutedText}" Margin="12,0"
                           VerticalAlignment="Center" IsHitTestVisible="False"
                           Visibility="{Binding Search, Converter={StaticResource HasValue2Vis}, ConverterParameter=invert}"/>
            </Grid>

            <ListBox x:Name="ItemList" Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}"
                     Background="Transparent" BorderThickness="0" MouseDoubleClick="OnItemDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Title}" Padding="4,5" Foreground="{DynamicResource Brush.Text}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</Window>
