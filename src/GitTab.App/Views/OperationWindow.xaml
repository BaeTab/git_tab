<Window x:Class="GitTab.App.Views.OperationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:GitTab.App.Localization"
        Title="{Binding Title}"
        Icon="pack://application:,,,/GitTab;component/Assets/gittab.ico"
        Width="600" Height="440" MinWidth="440" MinHeight="300"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Brush.Window}"
        FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text}"/>
            <TextBlock Text="{Binding RepositoryName}" Style="{StaticResource MutedText}" VerticalAlignment="Bottom" Margin="10,0,0,2"/>
        </StackPanel>

        <ProgressBar Grid.Row="1" Height="4" Margin="0,12,0,0" IsIndeterminate="{Binding IsRunning}"
                     Background="Transparent" BorderThickness="0" Foreground="{DynamicResource Brush.Accent}"
                     Visibility="{Binding IsRunning, Converter={StaticResource Bool2Vis}}"/>

        <!-- output -->
        <Border Grid.Row="2" Margin="0,12,0,0" Background="{DynamicResource Brush.SurfaceAlt}"
                BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="6">
            <TextBox Text="{Binding Output, Mode=OneWay}" IsReadOnly="True" BorderThickness="0" Background="Transparent"
                     Foreground="{DynamicResource Brush.Text}" FontFamily="Consolas" FontSize="12"
                     TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Padding="10"/>
        </Border>

        <!-- footer -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Status}" VerticalAlignment="Center"
                       Foreground="{DynamicResource Brush.TextMuted}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="{loc:Tr Common.Cancel}" MinWidth="92" Margin="0,0,8,0"
                        Command="{Binding CancelCommand}"
                        Visibility="{Binding IsRunning, Converter={StaticResource Bool2Vis}}"/>
                <Button Content="{loc:Tr Common.Close}" IsCancel="True" IsDefault="True"
                        MinWidth="92" Click="OnClose"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
